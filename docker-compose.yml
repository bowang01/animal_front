services:
  frontend:
    build: .
    container_name: animal-front
    restart: unless-stopped
    ports:
      - "127.0.0.1:18080:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      VUE_APP_BASE_API: /api
